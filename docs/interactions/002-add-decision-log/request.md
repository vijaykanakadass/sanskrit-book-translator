# Request

Every decision should be recorded into the repo itself. Anything which feels like a decision should be recorded. Also, every change should have a plan saved to the repo. Give names to the workflow rules so they can be referenced.
