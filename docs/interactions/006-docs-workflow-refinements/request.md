# Request

Several workflow and config refinements requested across the session:

1. Client and server should each have their own `.env` and `.env.example`
2. Increase PDF upload limit to 300MB
3. Plans should keep full detail, not be condensed summaries
4. Decisions should keep full detail, not be condensed summaries
5. The `docs/` folder is the single source of truth — the codebase should be re-creatable from docs alone
6. Paper Trail rule should say "Report" not "Summarize"
7. Add a `docs/changelog/` folder for small changes that don't warrant a plan or decision
