// Placeholder — will be replaced with a real implementation
export class PlaceholderTranslationService {
  async translate(sanskritText) {
    return {
      originalText: sanskritText,
      translatedText: "[Translation service not configured]",
    };
  }
}
